# Generated by generate.py. Do not edit by hand.
# Amp version: 0.0.1788134497-gb6ce09
# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: "amp"
description: "Amp CLI"
persistentflags:
  "--visibility=": "Set thread visibility (private, unlisted, workspace, group)"
  "-V, --version": "Print the version number and exit"
  "-v": "Alias for --version"
  "--notifications": "Enable notification alerts (audio locally, terminal bell over SSH or with AMP_FORCE_BEL; enabled by default when not in execute mode)"
  "--no-notifications": "Disable notification alerts (audio locally, terminal bell over SSH or with AMP_FORCE_BEL; enabled by default when not in execute mode)"
  "--color": "Enable color output (enabled by default if stdout and stderr are sent to a TTY)"
  "--no-color": "Disable color output (enabled by default if stdout and stderr are sent to a TTY)"
  "--settings-file=": "Custom settings file path (overrides the default location ~/.config/amp/settings.json)"
  "--log-level=": "Set log level (parent, children, category, sinks, parentSinks, filters, lowestLevel, contextLocalStorage)"
  "--log-file=": "Set log file location (overrides the default location ~/.cache/amp/logs/cli.log)"
  "--ide": "Enable IDE connection (default). When enabled, Amp automatically includes your open IDE's file and text selection with every message."
  "--no-ide": "Disable IDE connection"
  "--mcp-config=": "JSON configuration or file path for MCP servers to merge with existing settings"
  "-m, --mode=": "Set the agent mode (low, medium, high, ultra, or a plugin mode by key or label, case-insensitive) \u2014 controls the model, system prompt, and tool selection"
  "--features=": "Enable a thread feature (known: fast, Faster serving for supported models, billed at a premium; pro, GPT-5.6 Pro mode (OpenAI API only, not supported by ChatGPT subscriptions))"
  "--fast": "Run new threads in Fast mode (faster serving for supported models, billed at a premium); alias for --features fast"
  "--no-tui": "Run Amp without a TUI: serve remote thread creation requests for the current directory. Use --runner-id <id> to set a stable runner ID."
  "--remote-control-terminal": "Enable terminal access from ampcode.com for remote-controlled threads"
  "--no-remote-control-terminal": "Disable terminal access from ampcode.com for remote-controlled threads"
  "-x, --execute?": "Use execute mode, optionally with user message. In execute mode, agent will execute provided prompt (either as argument, or via stdin). Only last assistant message is printed. Enabled automatically when redirecting stdout."
  "-ox, --orb-execute": "When used with -x/--execute, execute in an async orb on the Amp server. The -ox shorthand enables both options. With 'threads continue', sends the message to the existing thread so it runs on that thread\u2019s own remote executor."
  "--project?": "With --orb-execute, run the new orb thread on this Amp project (namespace/name, owner/repo, or repository URL) instead of inferring it from the current directory\u2019s Git remotes."
  "--orb-size=": "With --orb-execute, run the new orb using this size instead of the project default."
  "--title=": "With --execute, set the title of the new thread before starting the agent."
  "--stream-json": "When used with --execute, output in Claude Code-compatible stream JSON format instead of plain text."
  "--stream-json-thinking": "Include thinking blocks in stream JSON output (non-Claude Code extension). Implies --stream-json."
  "--stream-json-input": "Read JSON Lines user messages from stdin. Requires both --execute and --stream-json."
  "--plugin-ready-timeout?": "When used with --execute, how long to wait for plugins to become ready before running the turn, so plugin lifecycle hooks (agent.start/agent.end) are not skipped. Off unless this flag is set; the bare flag uses 10. Max 300. Use 0 to disable."
  "--no-archive-after-execute": "When used with --execute on a new thread or with the review command, leave the thread unarchived after the command finishes."
  "-l, --label=": "Add a label to the thread created or continued by this command. Repeat the flag for multiple labels."
completion:
  flag:
    "visibility": ["private", "unlisted", "workspace", "group"]
    "mode": ["low", "medium", "high", "ultra"]
    "features": ["fast\tFaster serving", "pro\tGPT-5.6 Pro mode"]
commands:
  - name: "version"
    description: "Print the version number and exit"
    flags:
      "-h, --help": "display help for command"
  - name: "logout"
    description: "Log out by removing stored API key"
  - name: "login"
    description: "Log in to Amp"
  - name: "orb"
    description: "Commands for Amp-managed orbs"
    commands:
      - name: "system-metrics"
        description: "Print historical orb system metrics for a thread URL or ID"
      - name: "portal"
        description: "Print the portal URL for an existing process; use \"amp orb service start\" for long-lived servers"
        flags:
          "--thread=": "Thread URL or ID (defaults to AMP_THREAD_ID)"
          "--domain=": "Portal domain (defaults to AMP_PORTAL_DOMAIN or onamp.dev)"
          "--name=": "Portal manifest name; writes .amp/portals/<name>.json (defaults to portal-<port>)"
          "--title=": "Portal tab title; writes the portal manifest .amp/portals/<name>.json (defaults to the manifest name)"
          "--description=": "Portal tab description shown as a note next to the link; written to the portal manifest"
          "--hostname=": "Set the portal's public hostname (a full managed hostname like <name>--<namespace>.onamp.dev or a custom domain) before printing the URL; pass 'auto' to restore the auto-generated hostname. The target may be omitted when exactly one portal is running."
          "--take-over": "Move a hostname from another thread portal; requires --hostname"
        commands:
          - name: "access"
            description: "Set portal access"
      - name: "service"
        aliases: ["services"]
        description: "Manage long-lived services (dev servers, watchers) as supervised processes that"
        commands:
          - name: "ensure"
            description: "Ensure every service declared in .amp/services.yaml is running and exposed at its portal URLs"
            flags:
              "--json": "Print machine-readable JSON results"
          - name: "start"
            description: "Start an undeclared ad-hoc supervised service, e.g.: amp orb service start preview --command 'pnpm dev'"
            flags:
              "--command=": "Shell command to run; it runs with $PORT set (and $PUBLIC_URL when --portal) and should listen on $PORT"
              "--cwd=": "Working directory for the command (defaults to the current directory)"
              "--port=": "Local port for $PORT (defaults to the running service\u2019s current port, else a free port)"
              "--portal": "Expose $PORT at a public portal URL: waits for it to listen, autodetects HTTP vs HTTPS, writes the portal manifest .amp/portals/<name>.json, and prints the portal URL"
              "--title=": "Portal tab title (defaults to the service name; requires --portal)"
              "--description=": "Portal tab description shown as a note next to the link (requires --portal)"
          - name: "restart"
            description: "Restart a declared service from .amp/services.yaml or reload an existing ad-hoc service"
          - name: "stop"
            description: "Stop a service"
          - name: "status"
            description: "Show supervisor status for a service"
          - name: "logs"
            description: "Show recent logs for a service"
            flags:
              "-n, --lines=": "Number of log lines to show"
          - name: "list"
            description: "List orb services"
      - name: "restart-processes"
        description: "Refresh environment variables and restart all processes in an orb thread"
        flags:
          "--thread=": "Thread URL (defaults to AMP_THREAD_ID)"
      - name: "id-token"
        description: "Mint a short-lived OIDC token with a caller-chosen audience and print it to stdout"
        flags:
          "--audience=": "The aud claim of the minted token"
          "--ttl-seconds=": "Token lifetime in seconds (60-3600)"
          "--subject-scope=": "Subject scope of the minted token (project, thread, user, or workspace; default: thread)"
  - name: "portal"
    description: "Commands for accessing Amp portal URLs"
    commands:
      - name: "login-url"
        description: "Mint a single-use login URL (valid ~60s) that signs a non-browser client into an Amp portal URL; fetching"
  - name: "clone"
    description: "Clone an Amp-hosted Project, Personal Plugins or Skills repository, or Workspace Plugins"
    flags:
      "--no-git-setup": "Do not configure the Git credential helper automatically (or set AMP_NO_GIT_SETUP=1)"
  - name: "git-setup"
    description: "Configure the Amp Git credential helper"
    flags:
      "--uninstall": "Remove only the Amp Git credential helper configuration"
  - name: "files"
    description: "Download files from Amp"
    commands:
      - name: "get"
        description: "Download a private Amp attachment"
        flags:
          "-o, --output=": "Write the attachment to this path"
  - name: "inference-test"
    description: "Run a small model inference, optionally through a specific provider key"
    flags:
      "--thread=": "Owned actor-backed thread used for billing context"
      "--provider-model=": "Provider/model ID"
      "--provider-key=": "Specific model provider key UUID (Amp staff only)"
  - name: "top"
    description: "Show live active threads"
    flags:
      "--stream-jsonl": "Stream a JSON line whenever the thread list changes (output schema is EXPERIMENTAL)"
  - name: "last"
    aliases: ["l"]
    description: "Continue the last thread"
  - name: "report-bug"
    aliases: ["report", "diagnostics"]
    description: "Report a bug with diagnostics from the amp --no-tui runner"
  - name: "threads"
    aliases: ["t", "thread"]
    description: "Manage threads"
    flags:
      "--include-archived": "Include archived threads in the list"
      "--limit=": "Maximum number of threads to return"
      "--offset=": "Number of threads to skip"
    commands:
      - name: "new"
        aliases: ["n"]
        description: "Create a new thread"
        flags:
          "--visibility=": "Set thread visibility (private, unlisted, workspace, group)"
        completion:
          flag:
            "visibility": ["private", "unlisted", "workspace", "group"]
      - name: "continue"
        aliases: ["c"]
        description: "Continue an existing thread"
        flags:
          "--last": "Continue the last thread for the current mode directly"
          "--pick": "Pick a thread interactively from a list (DEPRECATED: picker is now the default)"
      - name: "list"
        aliases: ["l", "ls"]
        description: "List all threads"
        flags:
          "--include-archived": "Include archived threads in the list"
          "--installation-id=": "Only list threads for a specific installation ID"
          "--limit=": "Maximum number of threads to return"
          "--offset=": "Number of threads to skip"
          "--json": "Output as JSON"
      - name: "usage"
        description: "Show usage information for a thread"
        flags:
          "--details": "Show detailed credit, token, model, and tool usage"
          "--start=": "Start of the usage range as an ISO 8601 timestamp"
          "--end=": "Exclusive end of the usage range as an ISO 8601 timestamp"
      - name: "visibility"
        aliases: ["v"]
        description: "Show or set default visibility for this repository"
      - name: "search"
        aliases: ["find"]
        description: "Search threads"
        flags:
          "-n, --limit=": "Maximum number of threads to return"
          "--offset=": "Number of results to skip (for pagination)"
          "--json": "Output as JSON"
      - name: "label"
        description: "Add labels to a thread"
      - name: "share"
        aliases: ["s"]
        description: "Share a thread"
        flags:
          "--visibility=": "Set thread visibility (private, unlisted, workspace, group)"
        completion:
          flag:
            "visibility": ["private", "unlisted", "workspace", "group"]
        commands:
          - name: "multiplayer"
            description: "Turn multiplayer on or off"
            commands:
              - name: "on"
                description: "Turn multiplayer on for the default duration (1 week)"
              - name: "off"
                description: "Turn multiplayer off"
              - name: "ttl"
                description: "Turn multiplayer on for a specific duration"
                flags:
                  "--minutes=": "Duration in minutes"
                  "--hours=": "Duration in hours"
                  "--days=": "Duration in days"
                  "--weeks=": "Duration in weeks"
      - name: "rename"
        aliases: ["r"]
        description: "Rename a thread"
      - name: "archive"
        description: "Archive a thread"
        flags:
          "--unarchive": "Unarchive the thread instead of archiving"
      - name: "delete"
        description: "Delete a thread"
      - name: "markdown"
        aliases: ["md"]
        description: "Render thread as markdown"
      - name: "export"
        description: "Export a thread as JSON"
      - name: "raw"
        aliases: ["raw-thread"]
        description: "Export raw actor thread data as JSON"
  - name: "tools"
    aliases: ["tool"]
    description: "Tool management commands"
    commands:
      - name: "list"
        aliases: ["ls"]
        description: "List all active tools (including MCP tools)"
        flags:
          "--json": "output as a single JSON array"
          "--mode=": "filter tools by agent mode (default: medium)"
        completion:
          flag:
            "mode": ["low", "medium", "high", "ultra"]
      - name: "show"
        description: "Show details about an active tool"
        flags:
          "--json": "output as a single JSON object"
          "--mode=": "filter tools by agent mode (default: medium)"
        completion:
          flag:
            "mode": ["low", "medium", "high", "ultra"]
  - name: "plugins"
    aliases: ["plugin"]
    description: "Manage project, system, personal, and workspace plugins"
    commands:
      - name: "add"
        description: "Install a plugin from a URL"
        flags:
          "--auto-update": "Automatically update this plugin when plugins are loaded"
          "--target=": "Where to put this plugin: system (default, ~/.config/amp/plugins) or workspace (.amp/plugins)"
      - name: "remove"
        aliases: ["rm"]
        description: "Remove an installed plugin"
        flags:
          "--target=": "Only remove from this location: system (~/.config/amp/plugins) or workspace (.amp/plugins). Defaults to searching both."
      - name: "update"
        description: "Update installed plugins or one imported shared plugin"
        flags:
          "--force": "Replace committed local changes to an imported plugin"
      - name: "list"
        aliases: ["ls"]
        description: "List loaded project, system, personal, and workspace plugins"
      - name: "import"
        description: "Import a shared personal plugin into a plugin repository checkout"
        flags:
          "--repository=": "Personal or Workspace Plugins repository checkout"
          "--overwrite": "Replace a plugin with the same file name"
      - name: "repositories"
        aliases: ["repos"]
        description: "List available Personal Plugins and Workspace Plugins repositories"
        flags:
          "--json": "Output repositories as JSON"
      - name: "show-agent-options"
        description: "Show models and built-in tool names available to plugin agents"
        flags:
          "--json": "Output metadata as JSON"
      - name: "exec"
        description: "Execute a plugin with a given event"
        flags:
          "--data=": "JSON event data"
      - name: "show-docs"
        description: "Output the plugin API documentation understood by this version of Amp"
  - name: "skill"
    aliases: ["skills"]
    description: "Manage skills from shared references, Git repositories, or local paths"
    commands:
      - name: "add"
        description: "Install skills from a source"
        flags:
          "--target=": "Target directory for skills"
          "--global": "Install to global skills directory (~/.config/agents/skills/)"
          "--overwrite": "Overwrite existing skill with the same name"
          "--name=": "Install with a custom local name"
      - name: "list"
        aliases: ["ls"]
        description: "List all available skills"
        flags:
          "--json": "Output as JSON"
      - name: "remove"
        aliases: ["rm"]
        description: "Remove an installed skill"
        flags:
          "--target=": "Target directory for skills"
      - name: "info"
        description: "Show information about a skill"
        flags:
          "--json": "Output as JSON"
      - name: "repositories"
        aliases: ["repos"]
        description: "List available Personal Skills and Workspace Skills repositories"
        flags:
          "--json": "Output repositories as JSON"
  - name: "permissions"
    aliases: ["permission"]
    description: "Manage permissions"
    commands:
      - name: "list"
        aliases: ["ls"]
        description: "List permissions"
        flags:
          "--json": "output as JSON"
          "--builtin": "output only builtin permissions"
          "-w, --workspace": "List workspace permissions instead of global"
      - name: "test"
        description: "Test permissions"
        flags:
          "-c, --context=": "Execution context (thread or subagent)"
          "-t, --thread-id=": "Thread URL or ID for evaluation"
          "--json": "output as JSON"
          "-q, --quiet": "suppress output, only use exit status"
          "-w, --workspace": "Test against workspace permissions instead of global"
      - name: "edit"
        description: "Edit permissions"
        flags:
          "-w, --workspace": "Edit workspace permissions instead of global"
      - name: "add"
        description: "Add permission rule"
        flags:
          "--to=": "Program to delegate decision to for delegate action"
          "--context=": "Limit to specific context (thread or subagent)"
          "-w, --workspace": "Add to workspace permissions instead of global"
  - name: "mcp"
    description: "Manage MCP servers"
    commands:
      - name: "add"
        description: "Add an MCP server configuration"
        flags:
          "--env=": "Environment variables as KEY=VALUE pairs (repeatable)"
          "--header=": "HTTP headers as KEY=VALUE pairs for URL-based MCP servers (repeatable)"
          "--workspace": "Target workspace settings instead of global settings"
      - name: "list"
        description: "List available MCP servers"
        flags:
          "--json": "Output as JSON"
      - name: "remove"
        description: "Remove an MCP server configuration"
      - name: "oauth"
        description: "Manage OAuth authentication for MCP servers"
        flags:
          "-h, --help": "display help for command"
      - name: "doctor"
        description: "Check MCP server status"
      - name: "approve"
        description: "Approve a workspace MCP server"
  - name: "config"
    description: "Manage Amp configuration"
    commands:
      - name: "edit"
        description: "Open the Amp settings file in $EDITOR"
        flags:
          "--global": "Edit user settings (default)"
          "--workspace": "Edit workspace settings"
      - name: "keymap"
        description: "List command keymap entries"
      - name: "model-providers"
        description: "Manage model provider keys and subscriptions"
        flags:
          "--json": "print the complete non-secret connection data as JSON"
          "--workspace": "list keys of your current workspace instead of your user"
        commands:
          - name: "list"
            description: "List model provider keys and subscriptions"
            flags:
              "--json": "print the complete non-secret connection data as JSON"
              "--workspace": "list keys of your current workspace instead of your user"
          - name: "activate"
            description: "Activate a model provider key (deactivates any other active key of the same provider)"
          - name: "deactivate"
            description: "Deactivate a model provider key"
            flags:
              "-y, --yes": "Confirm subscription deactivation"
          - name: "show"
            description: "Show complete non-secret model provider connection data as JSON"
            flags:
              "--workspace": "read from your current workspace instead of your user"
          - name: "delete"
            description: "Delete a model provider key or subscription"
          - name: "test"
            description: "Test a model provider key or subscription"
          - name: "add-router"
            description: "Add an AI model router and select the models routed through it"
            flags:
              "--name=": "custom connection name"
              "--model-mapping=": "newline- or comma-separated provider/model patterns served by this connection, such as */* or openai/gpt-5.6-*; prefix a pattern with - to exclude it, and add \"-> provider-model-id\" after an exact model ID to override the model ID sent to the provider (the default when not given is all supported models)"
              "--api-key-file=": "read the API key from a file, or '-' for stdin"
              "--active": "activate the model router after creating it"
              "--no-active": "create the model router without activating it"
              "--personal": "add the model router to your personal account"
              "--workspace": "add the model router to your current workspace"
              "--api-format=": "custom-url API format: chat-completions, responses, anthropic-messages"
              "--base-url=": "custom-url base URL, using HTTPS"
              "--cloudflare-account-id=": "Cloudflare account ID"
              "--cloudflare-gateway-id=": "Cloudflare AI Gateway ID"
              "--workload-identity-provider=": "google-cloud-agent-platform workload identity pool provider that trusts Amp, as projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/POOL/providers/PROVIDER"
              "--google-cloud-project=": "google-cloud-agent-platform Google Cloud project ID to bill (default: the pool's project)"
              "--google-cloud-location=": "google-cloud-agent-platform location, a region ID or global"
              "--headers=": "custom request headers as JSON [name, value] pairs"
              "--query-params=": "custom-url query parameters as a JSON object"
              "--router-options=": "typed request options for the selected dedicated gateway"
          - name: "edit-router"
            description: "Edit an AI model router"
            flags:
              "--name=": "new connection name"
              "--model-mapping=": "new newline- or comma-separated provider/model patterns served by this connection; prefix a pattern with - to exclude it, and add \"-> provider-model-id\" after an exact model ID to override the model ID sent to the provider"
              "--clear-model-mapping": "reset the connection to its default models"
              "--api-key-file=": "read a replacement API key from a file, or '-' for stdin"
              "--api-format=": "new custom-url API format: chat-completions, responses, anthropic-messages"
              "--base-url=": "new custom-url base URL, using HTTPS"
              "--cloudflare-account-id=": "Cloudflare account ID"
              "--cloudflare-gateway-id=": "Cloudflare AI Gateway ID"
              "--workload-identity-provider=": "new google-cloud-agent-platform workload identity pool provider"
              "--google-cloud-project=": "new google-cloud-agent-platform Google Cloud project ID; pass \"\" to bill the pool project"
              "--google-cloud-location=": "new google-cloud-agent-platform location"
              "--headers=": "custom request headers as JSON [name, value] pairs"
              "--query-params=": "new custom-url query parameters as a JSON object"
              "--router-options=": "new typed request options for the dedicated gateway"
          - name: "setup-guide"
            description: "Explain how to obtain the credential an AI model router needs"
            flags:
              "--personal": "show the exact identity values for connections on your personal account"
              "--workspace": "show the exact identity values for connections on your current workspace"
          - name: "edit-key"
            description: "Edit a model provider API key name or model mapping"
            flags:
              "--name=": "new connection name"
              "--model-mapping=": "new newline- or comma-separated provider/model patterns served by this connection; prefix a pattern with - to exclude it, and add \"-> provider-model-id\" after an exact model ID to override the model ID sent to the provider"
              "--clear-model-mapping": "reset the connection to its default models Only the given flags change; other settings are kept."
          - name: "add-chatgpt-subscription"
            description: "Link your ChatGPT subscription to Amp as a model provider by signing in to OpenAI in your"
      - name: "vocabulary"
        description: "Manage personal dictation vocabulary"
        commands:
          - name: "show"
            description: "Show personal dictation vocabulary (default)"
          - name: "add"
            description: "Add terms without replacing existing vocabulary"
          - name: "set"
            description: "Replace personal dictation vocabulary; omit terms to clear it"
  - name: "domains"
    description: "Manage custom domains for sites"
    commands:
      - name: "list"
        description: "List custom domains, statuses, and required DNS records"
        flags:
          "--user": "Use domains on your personal account"
          "--workspace": "Use domains on your current workspace"
          "--json": "Print JSON output"
      - name: "add"
        description: "Add a custom domain"
        flags:
          "--user": "Use domains on your personal account"
          "--workspace": "Use domains on your current workspace"
          "--json": "Print JSON output"
      - name: "check"
        description: "Request an immediate DNS and provisioning status check"
        flags:
          "--user": "Use domains on your personal account"
          "--workspace": "Use domains on your current workspace"
          "--json": "Print JSON output"
      - name: "remove"
        aliases: ["rm"]
        description: "Remove a custom domain"
        flags:
          "--user": "Use domains on your personal account"
          "--workspace": "Use domains on your current workspace"
          "-y, --yes": "Confirm removal"
          "--json": "Print JSON output"
  - name: "projects"
    description: "Manage Amp projects"
    flags:
      "--json": "Print JSON output"
    commands:
      - name: "create"
        description: "Create a project for a repository"
        flags:
          "--orb-size=": "Orb size"
          "--ship-behavior=": "Ship behavior"
          "--custom-ship-prompt-file=": "Read the Custom Ship prompt from a file, or '-' for stdin"
          "--commit-author=": "Git commit author"
          "--icon=": "Project icon image"
          "--name=": "Project name. Defaults to the repository name."
          "--personal": "Create the project under your personal account"
          "--workspace": "Create the project under your current workspace (default when set)"
          "--json": "Print JSON output"
      - name: "update"
        description: "Update project settings by ID, [@]namespace/name, or repository"
        flags:
          "--orb-size=": "Orb size"
          "--ship-behavior=": "Ship behavior"
          "--custom-ship-prompt-file=": "Read the Custom Ship prompt from a file, or '-' for stdin"
          "--commit-author=": "Git commit author"
          "--description=": "Amp Store listing description"
          "--icon=": "Project icon image"
          "--listed-on-amp-store": "List the project on the Amp Store"
          "--no-listed-on-amp-store": "Remove the project from the Amp Store"
          "--pre-clone-script-file=": "Read the pre-clone script from a file, or '-' for stdin"
          "--pre-setup-script-file=": "Read the pre-setup script from a file, or '-' for stdin"
          "--json": "Print JSON output"
      - name: "list"
        description: "List projects accessible to you"
        flags:
          "--json": "Print JSON output"
      - name: "status"
        description: "Show the project matched by this directory's git remotes"
        flags:
          "--json": "Print JSON output"
      - name: "get"
        description: "Get a project by ID, [@]namespace/name, or repository"
        flags:
          "--json": "Print JSON output"
      - name: "snapshots"
        description: "Manage a project\u2019s shared orb snapshots"
        commands:
          - name: "list"
            description: "List shared orb snapshots by project and orb size"
            flags:
              "--json": "Print JSON output"
          - name: "delete"
            aliases: ["rm"]
            description: "Delete all shared orb snapshots, or one orb size with --resource"
            flags:
              "--resource=": "Delete only this orb size"
              "--json": "Print JSON output"
      - name: "gallery-media"
        description: "Manage a project\u2019s Amp Store gallery media"
        commands:
          - name: "get"
            description: "Print a project\u2019s Amp Store gallery media as JSON"
          - name: "set"
            description: "Set a project\u2019s Amp Store gallery media from a JSON array of {\"type\":\"image\"|\"video\",\"url\":\"<Amp artifact"
      - name: "delete"
        aliases: ["rm"]
        description: "Delete a project by ID, [@]namespace/name, or repository"
        flags:
          "-y, --yes": "Confirm deletion"
          "--json": "Print JSON output"
  - name: "apps"
    description: "Manage workspace apps"
    commands:
      - name: "deploy"
        description: "Deploy a directory to a workspace app"
        flags:
          "--command=": "Executable to start inside the bundle"
          "--arg=": "Argument passed to the executable (repeatable)"
          "--domain=": "Custom domain for this app"
          "--default-domain": "Return this app to its Amp-provided domain"
          "--port=": "Port exposed by the server"
          "--health-path=": "HTTP path used to verify the deployment"
          "--icon=": "App icon image"
          "--size=": "Sandbox size for this deployment"
      - name: "create"
        description: "Create a workspace app"
        flags:
          "--icon=": "App icon image"
      - name: "edit"
        description: "Edit a workspace app"
        flags:
          "--icon=": "App icon image"
      - name: "list"
        description: "List apps in a workspace"
      - name: "deployments"
        description: "List deployment history for an app"
      - name: "delete"
        description: "Delete a workspace app"
        flags:
          "--yes": "Confirm deleting the app and its deployment history"
  - name: "oauth-clients"
    description: "Manage workspace External API OAuth clients"
    commands:
      - name: "list"
        description: "List OAuth clients and client IDs"
        flags:
          "--workspace=": "Workspace namespace that owns the OAuth client"
      - name: "create"
        description: "Create a Sign in with Amp OAuth client"
        flags:
          "--type=": "OAuth client type"
          "--name=": "OAuth client name"
          "--description=": "OAuth client description"
          "--redirect-uri=": "Redirect URI (repeatable; first is the default)"
          "--scope=": "Delegated OAuth scope (repeatable)"
          "--workspace=": "Workspace namespace that owns the OAuth client"
      - name: "edit"
        description: "Edit a Sign in with Amp OAuth client"
        flags:
          "--name=": "OAuth client name"
          "--description=": "OAuth client description; pass an empty value to clear"
          "--redirect-uri=": "Replace redirect URIs (repeatable; first is the default)"
          "--scope=": "Replace delegated OAuth scopes (repeatable)"
          "--clear-scopes": "Remove all delegated OAuth scopes"
          "--workspace=": "Workspace namespace that owns the OAuth client"
  - name: "secrets"
    description: "Manage environment variables and secrets for orbs and Apps"
    commands:
      - name: "list"
        description: "List secret and environment variable names"
        flags:
          "--user": "Use your user-level secrets"
          "--workspace": "Use workspace secrets and environment variables"
          "--app=": "Use an App (workspace/app)"
          "--project?": "Use a project (ID, namespace/name, or repository); defaults to the current directory's project"
          "--json": "Print JSON output"
      - name: "history"
        description: "Show the change history of secrets and environment variables, including deletions (values are never shown)"
        flags:
          "--user": "Use your user-level secrets"
          "--workspace": "Use workspace secrets and environment variables"
          "--app=": "Use an App (workspace/app)"
          "--project?": "Use a project (ID, namespace/name, or repository); defaults to the current directory's project"
          "--json": "Print JSON output"
      - name: "get"
        description: "Get an environment variable value (secret values are never returned)"
        flags:
          "--user": "Use your user-level environment variables"
          "--workspace": "Use workspace secrets and environment variables"
          "--app=": "Use an App (workspace/app)"
          "--project?": "Use a project (ID, namespace/name, or repository); defaults to the current directory's project"
      - name: "set"
        description: "Add or update a secret or environment variable"
        flags:
          "--user": "Use your user-level secrets"
          "--workspace": "Use workspace secrets and environment variables"
          "--app=": "Use an App (workspace/app)"
          "--project?": "Use a project (ID, namespace/name, or repository); defaults to the current directory's project"
          "--data-file=": "Read the value from a file, or '-' for stdin"
          "--env": "Store as an environment variable"
          "--secret": "Store as a secret"
          "--json": "Print JSON output"
      - name: "delete"
        aliases: ["rm"]
        description: "Delete a secret or environment variable"
        flags:
          "--user": "Use your user-level secrets"
          "--workspace": "Use workspace secrets and environment variables"
          "--app=": "Use an App (workspace/app)"
          "--project?": "Use a project (ID, namespace/name, or repository); defaults to the current directory's project"
          "-y, --yes": "Confirm deletion"
          "--json": "Print JSON output"
  - name: "svc"
    description: "Call connected services through Amp"
    commands:
      - name: "gmail"
        description: "Call the connected Gmail account"
        commands:
          - name: "search_messages"
            description: "Search Gmail messages"
            flags:
              "--input=": "search_messages input as a JSON object"
          - name: "read_message"
            description: "Read one Gmail message"
            flags:
              "--input=": "read_message input as a JSON object"
          - name: "list_labels"
            description: "List Gmail labels"
          - name: "create_draft"
            description: "Create a Gmail draft"
            flags:
              "--input=": "create_draft input as a JSON object"
          - name: "send_message"
            description: "Send a Gmail message"
            flags:
              "--input=": "send_message input as a JSON object"
          - name: "send_draft"
            description: "Send a Gmail draft"
            flags:
              "--input=": "send_draft input as a JSON object"
          - name: "modify_labels"
            description: "Modify Gmail message labels"
            flags:
              "--input=": "modify_labels input as a JSON object"
  - name: "usage"
    description: "Show your current Amp usage and credit balance"
    flags:
      "--details": "Show detailed credit, token, and thread usage"
      "--start=": "Start of the usage range as an ISO 8601 timestamp"
      "--end=": "End of the usage range as an ISO 8601 timestamp"
  - name: "update"
    aliases: ["up"]
    description: "Update Amp CLI"
    flags:
      "--target-version=": "Update to a specific version"
      "--porcelain": "Print a machine-readable result to stdout: \"updated <version>\" or \"no update needed\""
      "-h, --help": "display help for command"
